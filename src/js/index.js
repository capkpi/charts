import * as Charts from './chart';

let capkpi     = { };

capkpi.NAME    = 'CapKPI Charts';
capkpi.VERSION = '2.0.0-rc22';

capkpi         = Object.assign({ }, capkpi, Charts);

export default capkpi;